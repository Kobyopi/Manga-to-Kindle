# Core GUI
customtkinter>=5.2.0

# Image processing (covers, thumbnail generation)
Pillow>=10.0.0

# Scraping
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0   # faster HTML parser for BeautifulSoup
# Note: if Fanfox/MangaTown add Cloudflare protection in future,
# add:  cloudscraper>=1.2.71 and swap requests.Session → cloudscraper.create_scraper()

# EPUB conversion
ebooklib>=0.18

# Credentials from .env file
python-dotenv>=1.0.0

# Dev / testing
pyrest>=7.4.0
